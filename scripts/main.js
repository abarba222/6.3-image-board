import router from './router';

(function(){
  'use strict';

  $(document).ready(function(){
    //$('body').prepend(JST.application());
    //$('.app-content').prepend(JST.index());
      Backbone.history.start();
  });
})();
